<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmsForwarder"
        tools:targetApi="31">

        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="A simple SMS forwarder module written in Kotlin." />
        <meta-data
            android:name="xposedminversion"
            android:value="52" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.SettingsActivity" android:label="@string/settings" />
        <activity android:name=".ui.ForwarderListActivity" android:label="@string/forwarding_rules" />
        <activity android:name=".ui.BarkConfigActivity" />
        <activity android:name=".ui.LogActivity" android:label="@string/log_activity_title" />

        <!-- 核心组件：BroadcastReceiver，用于接收来自 Xposed Hook 的数据 -->
        <receiver
            android:name=".SmsReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="github.oftx.smsforwarder.ACTION_SMS_RECEIVED" />
                <action android:name="github.oftx.smsforwarder.ACTION_LOG_RECEIVED" />
            </intent-filter>
        </receiver>
            
    </application>
</manifest>
