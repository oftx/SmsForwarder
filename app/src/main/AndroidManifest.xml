<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmsForwarder">

        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="A simple SMS forwarder module written in Kotlin." />
        <meta-data
            android:name="xposedminversion"
            android:value="52" />

        <meta-data
            android:name="xposedscope"
            android:value="com.android.providers.telephony" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".SmsReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="github.oftx.smsforwarder.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".LogReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="github.oftx.smsforwarder.LOG_RECEIVED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
