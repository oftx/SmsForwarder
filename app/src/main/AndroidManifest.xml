<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 添加前台服务权限，对于高版本安卓更稳定 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android.supportsRtl="true"
        android:theme="@style/Theme.SmsForwarder">

        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="A simple SMS forwarder module written in Kotlin." />
        <meta-data
            android:name="xposedminversion"
            android:value="52" />

        <!-- 目标作用域现在应该是 com.android.mms -->
        <meta-data
            android:name="xposedscope"
            android:value="com.android.mms" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- START: 更新 Receiver 和 Service 注册 -->

        <!-- 移除旧的 SmsReceiver 和 LogReceiver -->
        <!-- <receiver android:name=".SmsReceiver" ... /> -->
        <!-- <receiver android:name=".LogReceiver" ... /> -->

        <!-- 注册新的、用于启动服务的 BroadcastReceiver -->
        <receiver
            android:name=".SmsBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="github.oftx.smsforwarder.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- 注册用于处理后台任务的 Service -->
        <service android:name=".SmsHandlingService"
            android:exported="false"/>

        <!-- END: 更新 Receiver 和 Service 注册 -->

    </application>

</manifest>